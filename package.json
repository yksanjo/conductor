{
  "name": "conductor-cli",
  "version": "0.5.0",
  "description": "Read-only situational awareness across your live Claude Code sessions. Open one window, see what every other window is doing.",
  "bin": {
    "conductor": "cli.js",
    "conductor-cockpit": "server.js",
    "conductor-mcp": "mcp.js"
  },
  "scripts": {
    "start": "node scan.js",
    "serve": "node server.js",
    "mcp": "node mcp.js",
    "test": "node test.js && node fleet.test.js && node mcp.test.js && node manage.test.js && node server.test.js"
  },
  "keywords": ["claude-code", "cli", "agents", "orchestration", "dashboard"],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {}
}
